{
  "name": "netflix-clone-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^4.4.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}